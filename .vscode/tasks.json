{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "compilar",                    // Nombre de la tarea
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe", // Ruta del compilador
            "args": [
                "-Iinclude",                         // Incluye la carpeta con headers
                "src/main.cpp",                      // Aqui van todos los archivos cpp que quieres compilar
                "src/TemplateClass.cpp",
                "-o",
                "build/main.exe",                    // Esta es la ruta donde se creara el archivo .exe
                "-g",                                // Incluye información de depuración
                "-fdiagnostics-color=always"   // Colorea los mensajes de error
            ],
            "options": {
                "cwd": "${workspaceFolder}"        // Folder en el cual esta configurado tu proyecto, es ideal dejarlo como aqui
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
